databaseChangeLog:
  - changeSet:
      id: 'create-organization-admin-table'
      author: ezra
      preConditions:
        - onFail: MARK_RAN
        - not:
            - tableExists:
                tableName: organization_admin
      changes:
        - createTable:
            tableName: organization_admin
            columns:
              - column:
                  name: id
                  type: uuid
                  defaultValueComputed: ${uuid_function}
                  constraints:
                    primaryKey: true

              - column:
                  name: admin_id
                  type: uuid
                  constraints:
                    nullable: false
                    notNullConstraintName: organization_admin_admin_id_notnull

              - column:
                  name: organization_id
                  type: uuid
                  constraints:
                    nullable: false
                    notNullConstraintName: organization_admin_organization_id_notnull

              - column:
                  name: start_on
                  type: timestamptz
                  defaultValueDate: ${now}

              - column:
                  name: end_on
                  type: timestamptz
