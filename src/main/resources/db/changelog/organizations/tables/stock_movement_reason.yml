databaseChangeLog:
  - changeSet:
      id: create-stock-movement-reason-table
      author: ezra
      labels: 1.0.0
      preConditions:
        - onFail: MARK_RAN
        - not:
            - tableExists:
                tableName: stock_movement_reason
      changes:
        - createTable:
            tableName: stock_movement_reason
            columns:
              - column:
                  name: id
                  type: uuid
                  defaultValueComputed: ${uuid_function}
                  constraints:
                    primaryKey: true

              - column:
                  name: reference_number
                  type: varchar(30)
                  constraints:
                    nullable: false
                    notNullConstraintName: stock_movement_reason_reference_number_notnull
                    unique: true
                    uniqueConstraintName: stock_movement_reason_reference_number_unique

              - column:
                  name: code
                  type: varchar(20)
                  constraints:
                    unique: true
                    uniqueConstraintName: stock_movement_reason_code_unique

              - column:
                  name: name
                  type: varchar(100)
                  constraints:
                    nullable: false
                    notNullConstraintName: stock_movement_reason_name_notnull

              - column:
                  name: description
                  type: text

              - column:
                  name: created_by_id
                  type: uuid
                  constraints:
                    nullable: false
                    notNullConstraintName: stock_movement_reason_created_by_id_notnull

              - column:
                  name: system_defined
                  type: boolean
                  defaultValueBoolean: false
                  constraints:
                    nullable: false
                    notNullConstraintName: stock_movement_reason_system_defined_notnull

              - column:
                  name: created_on
                  type: timestamptz
                  defaultValueDate: ${now}
