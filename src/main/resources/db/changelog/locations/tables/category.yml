databaseChangeLog:
  - changeSet:
      id: 'create-category-table'
      author: kenneth
      preConditions:
        - not:
            - tableExists:
                tableName: category
      changes:
        - createTable:
            tableName: category
            columns:
              - column:
                  name: id
                  type: uuid
                  defaultValueComputed: ${uuid_function}
                  constraints:
                    primaryKey: true

              - column:
                  name: global_id
                  type: uuid
                  defaultValueComputed: ${uuid_function}

              - column:
                  name: category_type
                  type: varchar

              - column:
                  name: category_name
                  type: varchar

              - column:
                  name: description
                  type: varchar

              - column:
                  name: created_by_id
                  type: uuid
                  constraints:
                    nullable: false
                    notNullConstraintName: category_created_by_id_notnull

              - column:
                  name: created_on
                  type: timestamptz
                  defaultValueDate: ${now}

              - column:
                  name: predecessor_of_id
                  type: uuid

              - column:
                  name: usage_count
                  type: bigint
                  defaultValueNumeric: '0'
